# Screenshots

当前 README 使用 `dashboard-home.png`，由本地生产构建和公开合成数据生成。后续可补充：

1. `source-detail.png` — 任意一类合成样例的详情页。
2. 更高分辨率的首页截图（需要时替换现有 `dashboard-home.png`）。

截图前请确认页面只显示公开合成数据，不包含本地路径、账号、访问令牌、真实个人信息或未授权原文。替换图片后，同步更新根目录 README 中的图片路径和说明。
